import lookupReference from './lookupReference';
import lookupSearch from './lookupSearch';

const lookupFunctions = Object.assign(
    {},
    lookupReference,
    lookupSearch
);

export default lookupFunctions;
